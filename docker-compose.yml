version: '3.8'

services:
  nexus-backend:
    build: ./backend
    ports:
      - "3001:3001"
    volumes:
      # Bind mount Docker socket to allow spawning sibling containers
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PORT=3001
